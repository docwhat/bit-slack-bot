display_information:
  name: Bit
  description: Tron's helpful sidekick
  background_color: "#07abab"
features:
  bot_user:
    display_name: bit
    always_online: true
  slash_commands:
    - command: /bit
      url: https://example.com/bitbot/slash/bit
      description: |
        TODO: describe what it can do
      usage_hint: /bit hello
      should_escape: false
oauth_config:
  scopes:
    bot:
      - commands
      - chat:write
      - chat:write.public
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
