_metadata:
  major_version: 1
  minor_version: 1
display_information:
  name: Bit
  description: Tron's helpful sidekick
  background_color: "#66fbfb"
settings:
  socket_mode_enabled: false
  interactivity:
    is_enabled: false
  #   request_url: https://example.com/slack/message_action
  # event_subscriptions:
  #   bot_events:
  #     - app_home_opened
features:
  app_home:
    home_tab_enabled: false
    messages_tab_enabled: false
    messages_tab_read_only_enabled: false
  bot_user:
    display_name: bit
  slash_commands:
    - command: /bit
      description: |
        TODO: describe what it can do
      usage_hint: /bit hello
      url: https://example.com/bitbot/slash/bit
oauth_config:
  scopes:
    bot:
      - commands
      - chat:write
      - chat:write.public
  # redirect_urls:
  #   - https://example.com/slack/auth
